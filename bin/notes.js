
	// Get target server
	// let target = await servers.getTarget(ns);	

	// console.log('Target server efficency:');
	// console.log(target);

	// console.log('n00dles efficency:');
	// console.log(await servers.getEfficency(ns, 'n00dles'));

	// console.log('Target server full info:');
	// console.log(ns.getServer(target.server));

	// console.log('Player info:');
	// console.log(ns.getPlayer());
	
	// return true;

	// // Calculate threads 
	// let threads = await servers.calculateThreads(ns, 'home', appInterface.gethackGrowWeakenScript() );

	// // Get script
	// let script = appInterface.gethackGrowWeakenScript();

	// // Hack target server
	// let hack = ns.run(script, threads, target.server);

	// // Wait 20sec and kill script
	// await ns.sleep(20000);
	// await ns.kill(hack);

	//await servers.access(ns);
	//await servers.purchaseServers(ns);